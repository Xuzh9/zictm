# 全局使用淘宝npm镜像（国内最快最稳定，包含所有npm包）
registry=https://registry.npmmirror.com

# SAP官方包仍使用npmjs.org（确保能获取到最新的@sap包）
@sap:registry=https://registry.npmjs.org

# 解决strict-ssl问题（企业环境常见）
strict-ssl=false

# 增加超时时间
timeout=120000

# 强制所有子模块使用根目录的npm配置
force=true